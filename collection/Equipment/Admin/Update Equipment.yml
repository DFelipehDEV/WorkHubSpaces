info:
  name: Update Equipment
  type: http
  seq: 2

http:
  method: PUT
  url: "{{BASEURL}}/equipments/:id"
  params:
    - name: id
      value: 69e2086d16f830f47bfdc945
      type: path
  body:
    type: json
    data: |-
      {
          "name": "Camara Fotografica"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
