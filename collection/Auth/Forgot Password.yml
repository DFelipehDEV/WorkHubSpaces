info:
  name: Forgot Password
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASEURL}}/forgotpassword"
  body:
    type: json
    data: "{\r

      \    \"email\": \"thediogopcm@gmail.com\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
