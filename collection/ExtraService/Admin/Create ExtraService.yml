info:
  name: Create ExtraService
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASEURL}}/extraservices"
  body:
    type: json
    data: "{\r

      \    \"name\": \"Café\",\r

      \    \"description\": \"Desc\",\r

      \    \"price\": 20.2,\r

      \    \"available\": false\r

      }"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
