info:
  name: Create SpaceType
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASEURL}}/spacetypes"
  body:
    type: json
    data: |-
      {
          "name": "Casa Praia"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
