info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASEURL}}/users/:id"
  params:
    - name: id
      value: 69c68594ffcf478af71c4a4d
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
