info:
  name: Get All Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASEURL}}/users"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "4"
      type: query
      disabled: true
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
