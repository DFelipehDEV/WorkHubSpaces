info:
  name: Create Reservation
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASEURL}}/reservations"
  body:
    type: json
    data: |-
      {
          "spaceId": "69b2d3dc3ea89a511eb64a80",
          "startDate": "2025-12-09",
          "endDate": "2025-12-25",
          "status": 3,
          "obs": "",
          "extraServices": null
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "201"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
