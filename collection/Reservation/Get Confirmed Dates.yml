info:
  name: Get Confirmed Dates
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASEURL}}/confirmed-dates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
