info:
  name: Get All Reservations
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASEURL}}/reservations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
