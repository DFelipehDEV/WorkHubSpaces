info:
  name: Get All Reservations
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASEURL}}/reservations"
  params:
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "5"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
