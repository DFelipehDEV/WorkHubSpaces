info:
  name: Get Reservation
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASEURL}}/reservations/:id"
  params:
    - name: id
      value: 69c2bef735e07f733477d4ab
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
