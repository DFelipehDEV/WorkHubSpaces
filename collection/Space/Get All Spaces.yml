info:
  name: Get All Spaces
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASEURL}}/spaces"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
